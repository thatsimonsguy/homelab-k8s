apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: webhook-router
  namespace: argocd
spec:
  destination:
    namespace: webhook-router
    server: https://kubernetes.default.svc
  source:
    repoURL: 'https://github.com/thatsimonsguy/homelab-k8s.git'
    path: 'apps/webhook-router'
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true