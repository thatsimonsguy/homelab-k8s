apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "roll-with-matt.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "roll-with-matt.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "roll-with-matt.name" . }}
    spec:
      containers:
        - name: roll-with-matt
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80