apiVersion: v1
kind: Service
metadata:
  name: {{ include "roll-with-matt.fullname" . }}
spec:
  selector:
    app: {{ include "roll-with-matt.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80