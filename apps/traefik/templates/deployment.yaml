apiVersion: apps/v1
kind: Deployment
metadata:
  name: traefik
  namespace: traefik
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: traefik
  template:
    metadata:
      labels:
        app: traefik
    spec:
      serviceAccountName: traefik
      containers:
        - name: traefik
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "--api.insecure=true"
            - "--entrypoints.web.address=:80"
            - "--entrypoints.websecure.address=:443"
            - "--providers.kubernetescrd"
            - "--providers.kubernetesingress"
            - "--certificatesresolvers.letsencrypt.acme.email={{ .Values.certificatesResolvers.letsencrypt.acme.email }}"
            - "--certificatesresolvers.letsencrypt.acme.storage={{ .Values.certificatesResolvers.letsencrypt.acme.storage }}"
            - "--certificatesresolvers.letsencrypt.acme.httpChallenge.entryPoint=web"
          ports:
            - name: web
              containerPort: 80
            - name: websecure
              containerPort: 443
