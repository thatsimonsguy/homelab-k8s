apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: sitemark-db-secret
  namespace: sitemark-api
spec:
  encryptedData:
    # TODO: Replace with actual sealed secret
    # Generate using: echo -n 'your-password' | kubeseal --raw --from-file=/dev/stdin --namespace sitemark-api --name sitemark-db-secret
    # For now, using placeholder - MUST BE REPLACED BEFORE DEPLOYMENT
    password: PLACEHOLDER_SEALED_SECRET_DATA
  template:
    metadata:
      name: sitemark-db-secret
      namespace: sitemark-api
    type: Opaque
