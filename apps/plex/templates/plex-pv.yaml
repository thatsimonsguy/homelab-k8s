apiVersion: v1
kind: PersistentVolume
metadata:
  name: plex-pv
spec:
  capacity:
    storage: 11Ti
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /mnt/nas
  storageClassName: "nas"