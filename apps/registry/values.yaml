replicaCount: 1

image:
  repository: registry
  tag: 2
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5000

persistence:
  enabled: true
  size: 5Gi
  storageClass: "retain-default-storage"
