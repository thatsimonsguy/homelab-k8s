apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "career-site.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "career-site.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "career-site.name" . }}
    spec:
      containers:
        - name: career-site
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
