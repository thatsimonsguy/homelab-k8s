apiVersion: v1
kind: Service
metadata:
  name: {{ include "career-site.fullname" . }}
spec:
  selector:
    app: {{ include "career-site.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
