image:
  repository: 192.168.2.17:5000/ingest-gateway
  tag: v0.1.1
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  host: matthewpsimons.com
  path: /ingest
  pathType: Prefix
  tls: []

postgres:
  db: mydatabase # <- TODO: give this a real name https://github.com/thatsimonsguy/treddit/issues/4
  user: myuser # <- TODO: move to secret later https://github.com/thatsimonsguy/treddit/issues/4
  host: 192.168.2.20
  port: 5432
  passwordSecretName: postgresql-secret
  passwordSecretKey: password

queryTimeoutSec: 5
