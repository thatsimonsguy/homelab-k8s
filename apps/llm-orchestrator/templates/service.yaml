apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-orchestrator.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "llm-orchestrator.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
