image:
  repository: 192.168.2.17:5000/traceroute-site
  tag: "0.1.6"
  pullPolicy: IfNotPresent

service:
  port: 80

ingress:
  host: traceroutehealth.com
  class: traefik

# The trial request form posts to /trial-request on the same host; the mailform chart owns
# that path (apps/mailform/values.yaml) and routes it to the relay, never to this site.
