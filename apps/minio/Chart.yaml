apiVersion: v2
name: minio
description: MinIO wrapper chart for homelab (S3-compatible object store — aggsys DocMgmt rehearsals, AGG-162)
type: application
version: 0.1.0
appVersion: "2024"

dependencies:
  - name: minio
    version: 14.6.16
    repository: https://charts.bitnami.com/bitnami
